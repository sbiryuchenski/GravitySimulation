namespace sfml
{
    /*
     * NotAButton used for default values of some variables and does nothing
     * White variants of buttons (for example PW CW MW) used for identify a button
     * Иди нахуй
     */
    public enum Buttons

    {
        PW, PR, CW, CR, MW, MR, F3W, F3R, InfoW, InfoR, LMB, RMB, Scroll, NotAButton
    }
}
